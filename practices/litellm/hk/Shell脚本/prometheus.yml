# ============================================
# Prometheus — LiteLLM 指标采集
# ============================================
scrape_configs:
  - job_name: 'litellm'
    scrape_interval: 15s
    static_configs:
      - targets: ['litellm:4000']
